# Test: Indentation with tabs
# Expected: Should parse without errors
# Category: Indentation
# Description: Tests tab-based indentation

@build:
	echo "line 1"
	echo "line 2"
	block:
		if [ true ]; then
			echo "nested"
		fi
