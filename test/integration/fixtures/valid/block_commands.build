# Block commands example
.shell: bash

@generate:
	block:
		for i in 1 2 3; do
			echo "Item $i"
		done

@with_if:
	block:
		if [ -f Buildfile ]; then
			echo "Buildfile exists"
		else
			echo "No Buildfile"
		fi
